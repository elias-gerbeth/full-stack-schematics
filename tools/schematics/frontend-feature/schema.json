{
  "$schema": "http://json-schema.org/schema",
  "id": "feature",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Feature name, e.g. 'product'",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
